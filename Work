with database.snapshot() as snapshot:
    results = snapshot.execute_sql(sql)

    rows = list(results)

    if results.metadata and results.metadata.row_type:
        columns = [f.name for f in results.metadata.row_type.fields]
    else:
        columns = []

    json_rows = [dict(zip(columns, row)) for row in rows]

print(json.dumps(json_rows, indent=2, default=str))