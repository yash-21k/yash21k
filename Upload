const interval = setInterval(() => {
  const nextBtn = document.querySelector('button:not([disabled])');

  if (nextBtn && nextBtn.innerText.toLowerCase().includes('next')) {
    console.log('Next button enabled, clicking...');
    nextBtn.click();
    clearInterval(interval);
  }
}, 1000);