import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.TestPropertySource;
import org.springframework.test.context.junit4.SpringRunner;

public class PropertyFileReaderTest {

    @RunWith(SpringRunner.class)
    @ContextConfiguration(classes = {AMHPOEventsDataflowConfig.class})
    @TestPropertySource("classpath:props/app-sit.properties")
    public static class SitTest {
        @Test
        public void testSitConfig() {
            System.out.println("Running SIT config test");
            // call your pipeline build/validation logic here
        }
    }

    @RunWith(SpringRunner.class)
    @ContextConfiguration(classes = {AMHPOEventsDataflowConfig.class})
    @TestPropertySource("classpath:props/app-uat.properties")
    public static class UatTest {
        @Test
        public void testUatConfig() {
            System.out.println("Running UAT config test");
        }
    }

    @RunWith(SpringRunner.class)
    @ContextConfiguration(classes = {AMHPOEventsDataflowConfig.class})
    @TestPropertySource("classpath:props/app-prod.properties")
    public static class ProdTest {
        @Test
        public void testProdConfig() {
            System.out.println("Running PROD config test");
        }
    }
}