public class ConfigValidationUtil {

    @SafeVarargs
    public static boolean validateNonNullMaps(Map<?, ?>... maps) {
        for (Map<?, ?> map : maps) {
            if (map == null || map.isEmpty()) {
                return false;
            }
        }
        return true;
    }
}