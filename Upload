import java.util.regex.*;

public static String extractRefValue(String input) {
    if (input == null) return null;

    Pattern p = Pattern.compile("REF:\\s*([^\\s/]+)");
    Matcher m = p.matcher(input);

    return m.find() ? m.group(1) : null;
}